

# Voice-Activated AI Assistant with Wake Word Detection

## Overview
Add an always-listening wake word detection system (like "Hey Love") that activates the AI assistant from any screen in the app. When the wake word is detected, a voice overlay appears, transcribes the user's speech, and sends it to LoveBot.

## Approach

We'll use the **