

## Fullscreen Mode Toggle

Add a fullscreen toggle using the browser's **Fullscreen API** (`document.documentElement.requestFullscreen()` / `document.exitFullscreen()`). This hides the browser chrome (address bar, toolbar) on supported browsers.

### Implementation

1. **Create `use